import {Text} from "react-native";
import {SafeAreaView} from "react-native-safe-area-context";

const Ride = () => {
	return(
		<SafeAreaView>
			<Text style={{color: '#fff'}}>Ride</Text>
		</SafeAreaView>
		);
}

export default Ride;